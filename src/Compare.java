
import java.util.Comparator;


/**
 * Celem klasy Compare jest jej wykorzystanie do posortowania kolejki priorytetowej
 * złożonej z objektów NODEINFO bazując na parametrze PATHCOST (koszcie ścieżki) lub
 * koszcie przechowywanym w każdym węźle
 *
 * */
class Compare implements Comparator<NodeInfo>{
	@Override
    public int compare(NodeInfo n1, NodeInfo n2){
		return 0;
    }
	
}